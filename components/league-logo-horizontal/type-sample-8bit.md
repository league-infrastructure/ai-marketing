# League Logo — Horizontal — type-sample-8bit (prompt)

Paired with `type-sample-8bit.png` — 8-bit shaded type sample — grayscale, AMAZING shading layers, bolts + spaceships

- project: `League-Logo-Horizontal`  ·  iteration: 7
- style: `type-sample-8bit`  ·  composition: `type-sample`  ·  component: `league-logo-horizontal`  ·  layout: `type-sample`
- model: `chatgpt-image-latest`
- reference images: `/Volumes/Proj/proj/league-projects/infrastructure/marketing/components/masthead/cover-8bit-masthead.png`

---

## Full assembled prompt

# Art Style

Render as a GRAYSCALE 8-bit / pixel-art structural sample. This is a type sample — it documents
the STRUCTURE of a component rendered in the video-game (8-bit) style — but unlike the plain
type sample it deliberately INCLUDES tonal shading (in gray tones) so the layered pixel shading
of the lettering is demonstrated. No color: black, white, and grays only.

## Rendering
- **Grayscale only.** Black, white, and gray tones — NO color of any kind. Shading is REQUIRED
  here, but expressed purely as gray tones.
- **8-bit / pixel art:** Everything is built from square pixels with hard, stair-stepped edges,
  like a retro arcade game. Blocky and chunky — no smooth curves and no anti-aliasing.
- **Show the SHADING structure:** The dominant word (e.g. "AMAZING") is rendered with clear
  horizontal SHADING BANDS running light at the top to dark at the bottom (light gray → mid
  gray → dark gray), PLUS a hard offset pixel drop-shadow / extrude, so the layered 8-bit depth
  of the letters is unmistakable. Demonstrating those shading layers is the whole point.
- **Outlines:** Bold blocky pixel outlines (a dark-gray or black keyline) around the lettering.
- **Background:** A dark (near-black) starfield — scattered single-pixel white/gray "stars",
  like a space arcade screen.

## 8-bit motifs (structural, grayscale)
- Pixel LIGHTNING BOLTS flanking the top line.
- Small pixel SPACESHIPS (retro space-invader / Galaga-style) flanking the top line.
- Keep these as clean blocky grayscale pixel shapes.

## Purpose
A structural sample that documents BOTH the element layout AND the layered pixel shading of the
component in the 8-bit style — the grayscale reference for re-skinning it in full arcade color
later.

# Composition

A single component isolated and centered on a plain white field, presented as a structural
layout sample.

## Layout
- ONE component (the logo / badge / masthead) centered, filling most of the frame with generous
  white margins around it.
- Nothing else in the frame — no scene, no extra graphics, no caption, no border.
- Elements are arranged exactly per the component's structure description so their placement
  reads clearly: text stacking, lightning bolts, rules, and highlight shapes.

## Feel
Clean, precise, diagrammatic — a spec sheet that documents the component's structure.

# Component

The horizontal lock-up of the League of Amazing Programmers wordmark: three centered, stacked
lines of bold condensed caps with flanking lightning bolts. This describes the STRUCTURE
(element placement and proportion) only — color and finish come from whichever style the
component is rendered in.

## Elements & placement (top to bottom, centered)
1. **Line 1 — "THE LEAGUE OF" (all on ONE line):** "LEAGUE" is the main word of this line,
   centered and the largest word on the line. The small word "THE" sits immediately to its left
   and the small word "OF" immediately to its right, all on the same line. Each small word
   ("THE" and "OF") is bracketed by a SHORT horizontal bar directly above it and directly below
   it. A small lightning bolt sits just outside each end of the whole line (one left, one right).
2. **Line 2 — "AMAZING":** the dominant line — the largest lettering, condensed caps, centered,
   with a slight energetic upward tilt. A small lightning bolt flanks each side of it.
3. **Line 3 — "PROGRAMMERS":** bottom line, condensed caps, centered, narrower than "AMAZING".

## Structural notes
- The wordmark has exactly THREE text lines — "THE LEAGUE OF", "AMAZING", "PROGRAMMERS". The
  word "OF" appears ONLY ONCE, on the top line; there is NO separate standalone "OF" line.
- The whole mark is enclosed in a curved banner/shield frame: a rounded rectangle whose top and
  bottom edges arch gently upward, drawn as a double outline (a thin inner rule just inside a
  heavier outer rule). All elements sit inside this frame.
- The ONLY horizontal bars in the mark are the short ones bracketing "THE" and "OF" (one above
  and one below each small word). There is NO horizontal rule between "AMAZING" and
  "PROGRAMMERS", and NO rules running out to the left/right sides of "OF".
- The lines are stacked with clear vertical separation — no line's lettering touches or overlaps
  the line above or below.
- "AMAZING" is roughly 1.6–2× the cap height of the two outer lines and is the visual anchor of
  the mark.
- Small lightning bolts flank the mark (one at each end of the top line and of the "PROGRAMMERS"
  line), drawn as short angled sparks kept clear of the type.
- The overall footprint is wider than tall (roughly 2.2 : 1), reading as a compact corner mark.
- All lettering is bold and condensed with heavy keylines; exact fills, colors, and finish are
  defined by the render style, not here.

# Scene

The horizontal "THE LEAGUE OF AMAZING PROGRAMMERS" wordmark logo, rendered as a black-and-white structural type sample per the component description.

# Layout

> Present the single component centered on a plain white background with generous margins. No
> border, no scene, no extra text beyond the component itself. Crisp, high-resolution line art
> suitable as a reusable structural reference sheet.

# Additional Direction

Convert the ATTACHED 8-bit masthead reference into a GRAYSCALE structural type sample. Keep its 8-bit pixel look exactly: the chunky pixel lettering "THE LEAGUE OF" / "AMAZING" / "PROGRAMMERS"; the layered top-to-bottom SHADING BANDS on "AMAZING" (now light gray at the top → mid gray → dark gray at the bottom) WITH its hard offset pixel drop-shadow; the bold blocky pixel outlines; the pixel lightning bolts; the small pixel spaceships flanking the top line; and the dark starfield background.
Render EVERYTHING in GRAYSCALE — black, white, and grays only, NO color.
Do NOT add a badge frame. Leave OUT the arcade "HIGH SCORE / INSERT COIN" panel and the robot — show only the wordmark with its bolts, spaceships, and stars.
